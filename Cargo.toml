[package]
name = "nexus-metadata"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.38.0", features = ["full"] }
sqlx = { version = "0.8.0", features = ["runtime-tokio", "sqlite"] }